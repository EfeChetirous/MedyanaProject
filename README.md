# MedyanaProject

This is a sample spa web application. built with asp.net core 3.0 web api and angular.

The code first approach was used, it has shared resources, used attribute filters for logging, service layer and dependency injection.

Before debugging the project you must change connection string from appsettings.json file.

